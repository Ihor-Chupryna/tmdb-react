import { Outlet } from "react-router-dom";

const MovieListCard = () => {
    return (
        <div>
            MovieListCard
            <Outlet/>
        </div>
    );
};

export { MovieListCard };